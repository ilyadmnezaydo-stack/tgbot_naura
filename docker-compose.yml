services:
  bot:
    build: .
    env_file:
      - .env
    environment:
      APP_MODE: bot
    restart: unless-stopped

  api:
    build: .
    env_file:
      - .env
    environment:
      APP_MODE: api
      API_PORT: 8080
    ports:
      - "8080:8080"
    restart: unless-stopped
